#include "main.h"
/**
 * add - sums two integers and returns
 * the result.
 *
 * @m: number one.
 *
 * @n: number two.
 *
 * Return: Add of number one and number two.
 */
int add(int m, int n)
{
	return (m + n);
}
